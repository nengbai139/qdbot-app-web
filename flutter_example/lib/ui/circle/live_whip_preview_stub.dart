import 'package:flutter/material.dart';

/// 非 Web 平台占位
class LiveWhipPreview extends StatelessWidget {
  const LiveWhipPreview({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
